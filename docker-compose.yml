services:
  dockerlock:
    build:
      context: .
      dockerfile: Dockerfile
    image: wfrankel55/group30-522:latest
    container_name: group30-522
    ports:
      - "8888:8888"
    volumes:
      - ./:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true